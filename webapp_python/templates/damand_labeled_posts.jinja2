{
	"posts":[
		{% for post, label in posts %}
		{
			"id":"{{ post.id }}",
			"title": "{{ post.title.decode('utf-8') }}",
			"text": "{{ post.text.decode('utf-8') }}",
			"label":  "{{ label }}"
		}
			{% if not loop.last %}
			,
			{% endif %}
		{% endfor %}
	]
}
