{
	"posts": [
		{% for post, conf in posts %}
		{
			"id":"{{ post.id }}",
			"title": "{{ post.title.decode('utf-8') }}",
			"text": "{{ post.text.decode('utf-8') }}",
			"confidence": {
				"category": "{{ conf.category }}",
				"conf": {{ conf.conf }}
			}
		}
			{% if not loop.last %}
			,
			{% endif %}
		{% endfor %}
	]
}
